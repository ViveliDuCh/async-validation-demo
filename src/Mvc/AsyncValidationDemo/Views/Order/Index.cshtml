@model Order
@{ ViewData["Title"] = "Order Validation"; }

<h3>IAsyncValidatableObject — Order Validation</h3>
<p>
    <code>Order</code> combines <code>[AsyncProductExists]</code>,
    <code>[MaxOrderValue]</code>, <code>[AsyncInventoryCheck]</code>, and
    <code>IAsyncValidatableObject</code>. The controller awaits
    <code>Validator.TryValidateObjectAsync()</code> so both sync and async business rules run
    without blocking the request thread.
</p>
<p class="text-muted">
    Try <code>Unknown</code> for the product, or push the total above <code>$50,000</code> /
    <code>$100,000</code>.
</p>

@if (ViewBag.SuccessMessage is not null)
{
    <div class="alert alert-success">@ViewBag.SuccessMessage</div>
}

<form asp-action="Index" method="post" class="col-md-6">
    <div asp-validation-summary="All" class="text-danger mb-3"></div>

    <div class="mb-3">
        <label asp-for="ProductName" class="form-label"></label>
        <input asp-for="ProductName" class="form-control" />
        <span asp-validation-for="ProductName" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Quantity" class="form-label"></label>
        <input asp-for="Quantity" class="form-control" />
        <span asp-validation-for="Quantity" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="UnitPrice" class="form-label"></label>
        <input asp-for="UnitPrice" class="form-control" />
        <span asp-validation-for="UnitPrice" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Delay" class="form-label">I/O Delay (ms)</label>
        <input asp-for="Delay" class="form-control" />
        <span asp-validation-for="Delay" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Validate</button>
    <a asp-controller="Home" class="btn btn-outline-secondary ms-2">← Back</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
